# Планировщик Заказов

Современное веб-приложение для управления заказами по торговым точкам с использованием лимитов и остатков.

## Основные возможности

### 📊 Управление торговыми точками
- Создание, редактирование и удаление торговых точек
- Копирование лимитов при создании новой точки
- Просмотр всех точек на главной панели

### 🎯 Управление лимитами
- Добавление лимитов для отдельной точки или всех точек сразу
- Удаление лимитов из одной или всех точек
- Простой формат ввода: `Товар - число`
- Поддержка массового добавления через многострочный ввод

### 🔍 Улучшенный алгоритм поиска товаров
Новый алгоритм **точно** различает похожие товары:
- ✅ «Дарксайд 25» и «Дарксайд 250» обрабатываются отдельно
- ✅ Токенизация: разбивка на слова и числа
- ✅ Точное совпадение токенов, особенно для чисел
- ✅ Приоритизация полных совпадений

### 🎛️ Система фильтров с формулами
Создавайте выражения для фильтрации позиций заказа:

**Доступные переменные:**
- `Лимиты` - значение лимита товара
- `Остаток` - остаток на складе
- `Заказ` - рассчитанный заказ

**Операции:** `+`, `-`, `*`, `/`, `>`, `<`, `>=`, `<=`, `==`, `!=`

**Примеры фильтров:**
```
Заказ > Лимиты / 3          # Оставить только большие заказы
Лимиты * 2                  # Удвоить лимиты
Заказ != 1 or Лимиты != 2  # Исключить заказы 1 при лимите 2
Заказ >= 5                  # Минимальный заказ 5 единиц
```

### 📤 Загрузка остатков
- **Drag & Drop** - перетащите Excel файл в зону загрузки
- **Формат файла:** Excel (.xlsx, .xls) с колонками «Товар» и «Остаток»
- Автоматический расчет заказа: `Заказ = max(0, Лимит - Остаток)`
- Применение активных фильтров при обработке

### 📥 Экспорт результатов
- Автоматическая загрузка сформированного заказа в Excel
- Файл содержит: Товар, Остаток, Лимиты, Заказ
- Имя файла: `{Название_точки}_заказ.xlsx`

## Технический стек

### Backend
- **FastAPI** - современный асинхронный веб-фреймворк
- **MongoDB** - база данных для хранения точек, лимитов и фильтров
- **Pandas** - обработка Excel файлов
- **Motor** - асинхронный драйвер MongoDB

### Frontend  
- **React** - библиотека для создания UI
- **Shadcn UI** - современные компоненты интерфейса
- **Tailwind CSS** - утилитарный CSS-фреймворк
- **Axios** - HTTP клиент
- **React Router** - маршрутизация

## Установка и запуск

### Требования
- Python 3.11+
- Node.js 18+
- MongoDB

### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn server:app --reload --host 0.0.0.0 --port 8001
```

### Frontend
```bash
cd frontend
yarn install
yarn start
```

### Переменные окружения

**backend/.env:**
```
MONGO_URL=mongodb://localhost:27017
DB_NAME=order_planner
CORS_ORIGINS=*
```

**frontend/.env:**
```
REACT_APP_BACKEND_URL=http://localhost:8001
```

## Создание портативной версии (.exe)

### Подготовка
1. Установите PyInstaller:
```bash
pip install pyinstaller
```

2. Создайте единую точку входа `portable_app.py`:
```python
import subprocess
import webbrowser
import time
import os
from pathlib import Path

def start_mongodb():
    # Запуск встроенного MongoDB
    mongo_path = Path("mongodb/mongod.exe")
    if mongo_path.exists():
        subprocess.Popen([str(mongo_path), "--dbpath", "data"])

def start_backend():
    subprocess.Popen(["python", "backend/server.py"])

def start_frontend():
    # Используйте pre-built версию React
    subprocess.Popen(["python", "-m", "http.server", "3000"], 
                     cwd="frontend/build")

if __name__ == "__main__":
    print("Запуск Планировщика Заказов...")
    
    start_mongodb()
    time.sleep(2)
    
    start_backend()
    time.sleep(3)
    
    start_frontend()
    time.sleep(2)
    
    webbrowser.open("http://localhost:3000")
    
    print("Приложение запущено!")
    input("Нажмите Enter для выхода...")
```

3. Соберите приложение:
```bash
pyinstaller --onefile --name="OrderPlanner" portable_app.py
```

### Альтернатива: Electron
Для более профессиональной портативной версии рекомендуется Electron:

1. Установите Electron:
```bash
npm install -g electron
```

2. Создайте `electron-main.js`
3. Упакуйте с помощью electron-builder

## API Эндпоинты

### Точки
- `GET /api/stores` - Получить все точки
- `GET /api/stores/{id}` - Получить точку по ID
- `POST /api/stores` - Создать точку
- `PUT /api/stores/{id}` - Обновить точку
- `DELETE /api/stores/{id}` - Удалить точку

### Лимиты
- `POST /api/stores/{id}/limits` - Добавить/обновить лимиты
- `DELETE /api/stores/{id}/limits/{product}` - Удалить лимит

### Фильтры
- `GET /api/filters` - Получить все фильтры
- `POST /api/filters` - Создать фильтр
- `DELETE /api/filters/{id}` - Удалить фильтр

### Обработка
- `POST /api/process` - Обработать файл с остатками
  - Form data: `file` (Excel файл)
  - Query params: `store_id`, `filter_expressions` (JSON array)

## Алгоритм обработки

1. **Загрузка данных**: Чтение Excel файла с остатками
2. **Валидация**: Проверка наличия колонок «Товар» и «Остаток»
3. **Сопоставление**: Поиск лимитов для каждого товара (улучшенный алгоритм)
4. **Расчёт заказа**: `Заказ = max(0, Лимит - Остаток)`
5. **Фильтрация**: Удаление строк с `Заказ = 0`
6. **Применение фильтров**: Выполнение пользовательских выражений
7. **Экспорт**: Генерация Excel файла с результатами

## Примеры использования

### Создание точки с копированием лимитов
```javascript
const response = await axios.post('/api/stores', {
  name: 'Магазин №2',
  copy_from_id: 'store-id-1'
});
```

### Добавление лимитов во все точки
```javascript
const response = await axios.post('/api/stores/store-id-1/limits', {
  limits: [
    { product: 'Товар 1', limit: 10 },
    { product: 'Товар 2', limit: 5 }
  ],
  apply_to_all: true
});
```

### Обработка с фильтрами
```javascript
const formData = new FormData();
formData.append('file', excelFile);
formData.append('store_id', storeId);
formData.append('filter_expressions', JSON.stringify([
  'Заказ > Лимиты / 3',
  'Заказ != 1 or Лимиты != 2'
]));

const response = await axios.post('/api/process', formData, {
  responseType: 'blob'
});
```

## Отличия от старой версии

### ✅ Улучшения
1. **Веб-интерфейс** вместо Tkinter
2. **База данных** вместо Excel файлов для лимитов
3. **Улучшенный поиск** - точное различение похожих товаров
4. **Система фильтров** с математическими выражениями
5. **Современный UI** с drag & drop
6. **Удаление лимитов** из одной или всех точек
7. **Копирование лимитов** при создании точки

### 🔄 Сохранено
1. Формат входных данных (Excel с «Товар» и «Остаток»)
2. Логика расчета заказа
3. Формат выходного файла

## Лицензия

MIT License

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.